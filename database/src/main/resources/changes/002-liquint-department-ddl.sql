CREATE TABLE department (
                            id INT PRIMARY KEY,
                            name VARCHAR(255) NOT NULL,
                            manager_id INT,
                            location VARCHAR(255)
);